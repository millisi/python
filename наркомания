x=50
y=50
def setup():
    size(500,500)
    background(0)

def draw():
    #background(0)
    fill(random (0,255) ,random (0,255) ,random (0,255))
    global x,y
    x=x+1
    y=y+1
    circle (x,y,50)
